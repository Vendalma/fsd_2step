mixin colors (arr = {color,name, code})
  for color in arr
    .color-block
      .rectangle.color-block__rectangle(class=`rectangle_${color.color}`)
      .info.color-block__info
        span.info__name #{color.name}
        span.info__code #{color.code}
