mixin radio({name,text, margin=false, checked = false})
  .radio
    label(class='radio__label' class= margin? 'radio__label_margin' : '') #{text}
      input(type="radio" name=name class='radio__input' checked=checked)
      span.radio__mark
