mixin checkbox(options = {})
  -
    const {
      isTypeRich = false,
      checked=false,
      textWidth="medium",
      name,
      text,
      header
      } = options

    const textWidthTypes = {
      medium: "checkbox__text_width_medium",
      large: "checkbox__text_width_large",
    }

  .checkbox
    label.checkbox__label(class=isTypeRich? "checkbox__label_rich" : "") 
      input(type = "checkbox" class="checkbox__input" checked=checked name=name)
      span.checkbox__mark
      if isTypeRich
        span.checkbox__header #{header}
      span.checkbox__text(class=textWidthTypes[textWidth]
      class=isTypeRich? "checkbox__text_rich" : "") #{text}